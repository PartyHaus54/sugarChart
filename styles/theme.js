import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#FAE596',
      contrastText: '#173E43'
    },
    secondary: {
      main: '#3FB0AC',
      contrastText: '#173E43'
    }
  }
});

export default theme;
